---
phase: 02
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Header.astro
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-04
  - NAV-05
user_setup: []

must_haves:
  truths:
    - Navigation header displays on all pages with logo and links to Home, Products, About, Pricing, Contact
    - Active page highlighting works correctly across all routes
    - Navigation uses layout inheritance pattern (no per-page imports needed)
    - Internal routing uses Astro's file-based routing pattern (no config needed)
  artifacts:
    - path: src/components/Header.astro
      provides: Navigation header with semantic HTML structure
      contains: <nav element
      contains: Astro.url.pathname
      contains: navLinks array with 5 entries
  key_links:
    - from: src/layouts/Layout.astro
      to: src/components/Header.astro
      via: import statement
      pattern: import Header from '../components/Header.astro'
    - from: src/components/Header.astro
      to: Astro's file-based routing
      via: href attributes
      pattern: href=['\"]./[a-z-]+['\"]
---

<objective>
Implement desktop navigation header with links and active page highlighting using Astro's server-side route detection.

Purpose: Create persistent navigation that displays on all pages via layout inheritance, with active states determined during static build using Astro.url.pathname API.

Output: Enhanced Header.astro with navigation links (Home, Products, About, Pricing, Contact), active page highlighting, and semantic HTML structure.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-shared-components/02-CONTEXT.md
@.planning/phases/02-shared-components/02-RESEARCH.md
@src/components/Header.astro
@src/layouts/Layout.astro
@src/styles/design-tokens.css

<interfaces>
From src/styles/design-tokens.css:
```css
--color-indigo-600: #4f46e5;  /* Active link color per D-08 */
--color-gray-600: #52525b;    /* Inactive link color per D-08 */
--color-gray-200: #e4e4e7;    /* Border color */
```

From src/layouts/Layout.astro:
```astro
import Header from '../components/Header.astro';
const { title = "Bostrich" } = Astro.props;
```
Layout.astro already imports Header and wraps all pages with Layout, satisfying NAV-02 (layout inheritance pattern).

From src/components/Header.astro:
```astro
const { title = "Bostrich" } = Astro.props;
<header class="w-full border-b border-gray-200 bg-white">
  <div class="mx-auto max-w-7xl px-4 py-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between">
      <h1 class="text-xl font-bold text-gray-900 tracking-tight">{title}</h1>
    </div>
  </div>
</header>
```
Existing container pattern (max-w-7xl, px-4 sm:px-6 lg:px-8) must be preserved per D-02.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add desktop navigation links with active page highlighting</name>
  <files>src/components/Header.astro</files>
  <read_first>
    - src/components/Header.astro (current implementation with logo and container pattern)
    - src/styles/design-tokens.css (indigo-600 and gray-600 color values for active/inactive states)
    - src/layouts/Layout.astro (verify Header import pattern and props)
  </read_first>
  <action>
    Add navigation links to Header.astro with the following implementation:

    1. In the frontmatter, add:
       - `const currentPath = Astro.url.pathname;` (per D-07: use Astro.url.pathname for server-side detection)
       - `const isActive = (path: string) => currentPath === path;` (per D-09: exact path matching)
       - `const navLinks` array with 5 entries (per D-03):
         ```typescript
         const navLinks = [
           { href: '/', label: 'Home' },
           { href: '/products', label: 'Products' },
           { href: '/about', label: 'About' },
           { href: '/pricing', label: 'Pricing' },
           { href: '/contact', label: 'Contact' },
         ];
         ```

    2. In the template, after the `<h1>` element, add a `<nav>` element with:
       - Semantic HTML structure using `<ul>` and `<li>` elements (per D-01)
       - Hidden on mobile, visible on desktop: `class="hidden md:flex items-center space-x-8"`
       - Map over navLinks array to generate links:
         ```astro
         <nav>
           <ul class="hidden md:flex items-center space-x-8">
             {navLinks.map(link => (
               <li>
                 <a
                   href={link.href}
                   class={`text-sm transition hover:text-indigo-600 ${
                     isActive(link.href) ? 'text-indigo-600 font-semibold' : 'text-gray-600'
                   }`}
                 >
                   {link.label}
                 </a>
               </li>
             ))}
           </ul>
         </nav>
         ```
       - Conditional CSS classes: `text-indigo-600 font-semibold` for active, `text-gray-600` for inactive (per D-08)

    3. Preserve existing container pattern: `max-w-7xl px-4 py-4 sm:px-6 lg:px-8` (per D-02)
    4. Preserve existing logo/title display: `<h1 class="text-xl font-bold text-gray-900 tracking-tight">{title}</h1>`
  </action>
  <verify>
    <automated>grep -n "Astro.url.pathname" src/components/Header.astro && grep -n "navLinks" src/components/Header.astro && grep -n "text-indigo-600 font-semibold" src/components/Header.astro</automated>
  </verify>
  <done>Header.astro contains navLinks array with 5 entries (/, /products, /about, /pricing, /contact), uses Astro.url.pathname for route detection, applies text-indigo-600 font-semibold to active links and text-gray-600 to inactive links, and navigation is hidden on mobile (hidden md:flex)</done>
</task>

<task type="auto">
  <name>Task 2: Verify navigation displays via layout inheritance</name>
  <files>src/layouts/Layout.astro</files>
  <read_first>
    - src/layouts/Layout.astro (verify Header import pattern)
    - src/components/Header.astro (verify component structure)
  </read_first>
  <action>
    Verify that Layout.astro follows layout inheritance pattern:
    1. Check that Layout.astro imports Header with: `import Header from '../components/Header.astro';`
    2. Check that Layout.astro passes title prop to Header: `<Header title={title} />`
    3. Check that Layout.astro wraps all page content: `<main class="min-h-screen"><slot /></main>`
    4. Verify no per-page Header imports exist in src/pages/ (grep for "import Header" in pages directory)
  </action>
  <verify>
    <automated>grep -n "import Header" src/layouts/Layout.astro && grep -n "from '../components/Header.astro'" src/layouts/Layout.astro && (grep -r "import Header" src/pages/ || echo "No per-page Header imports found")</automated>
  </verify>
  <done>Layout.astro imports Header from ../components/Header.astro, passes title prop, wraps content with <main> and <slot/>, and src/pages/ contains no Header imports (layout inheritance pattern confirmed)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→HTML | Static HTML navigation renders in browser; no authentication or sensitive data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | S (Spoofing) | Navigation links | accept | Static site, no user input, no authentication required. Links are hardcoded in component. |
| T-02-02 | T (Tampering) | Active route detection | accept | Server-side detection (Astro.url.pathname) during static build, no client-side manipulation possible. |
| T-02-03 | R (Repudiation) | N/A | N/A | No logging or audit trail needed for static site navigation. |
| T-02-04 | I (Information disclosure) | N/A | N/A | No sensitive information exposed in navigation. |
| T-02-05 | D (Denial of service) | N/A | N/A | Static site hosted on Vercel/Cloudflare Pages with CDN, no DoS risk from navigation component. |
| T-02-06 | E (Elevation of privilege) | N/A | N/A | No authentication or authorization, no privilege elevation possible. |
</threat_model>

<verification>
Phase verification checks:
1. Header.astro imports are minimal (no external dependencies beyond Astro)
2. Navigation uses semantic HTML (<nav>, <ul>, <li>) per D-01
3. Active page highlighting uses server-side Astro.url.pathname API, no client-side JavaScript
4. Layout inheritance pattern confirmed (Header imported once in Layout.astro)
5. Container pattern preserved (max-w-7xl, px-4 sm:px-6 lg:px-8)
</verification>

<success_criteria>
1. Navigation header displays on all pages with 5 links (Home, Products, About, Pricing, Contact)
2. Active page highlighting works: current route link has text-indigo-600 font-semibold, others have text-gray-600
3. Navigation uses layout inheritance pattern (Header imported in Layout.astro, not per-page)
4. Navigation is hidden on mobile devices (hidden md:flex)
5. Container pattern matches existing design (max-w-7xl, px-4 sm:px-6 lg:px-8)
6. Semantic HTML structure with <nav>, <ul>, <li> elements
7. Exact path matching for active state (e.g., /products active on /products but not /products/agile-ladder)
</success_criteria>

<output>
After completion, create `.planning/phases/02-shared-components/02-01-SUMMARY.md`
</output>
