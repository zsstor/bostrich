---
phase: 02
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/components/Footer.astro
  - package.json
  - src/components/Header.astro
autonomous: true
requirements:
  - NAV-03
  - NAV-06
user_setup: []

must_haves:
  truths:
    - Footer component displays consistently on all pages with links, social media, and copyright
    - Mobile navigation menu (hamburger) toggles open/closed on mobile devices
    - Mobile menu uses smooth transition animations
    - Social media icons display using Lucide icons
    - Footer uses multi-column layout (1 column mobile, 3 columns desktop)
  artifacts:
    - path: src/components/Footer.astro
      provides: Multi-column footer with navigation, social links, and copyright
      contains: grid grid-cols-1 md:grid-cols-3
      contains: @lucide/astro imports
      contains: 3 column sections (navigation, social, legal)
    - path: src/components/Header.astro
      provides: Mobile menu with hamburger toggle and smooth transitions
      contains: hamburger button with aria-label
      contains: mobile menu dropdown with transition classes
      contains: <script tag with Vanilla JS toggle
    - path: package.json
      provides: @lucide/astro dependency
      contains: "@lucide/astro"
  key_links:
    - from: src/components/Footer.astro
      to: @lucide/astro package
      via: import statement
      pattern: from '@lucide/astro'
    - from: src/components/Header.astro
      to: Mobile menu state
      via: event listener in <script>
      pattern: addEventListener.*click
    - from: src/layouts/Layout.astro
      to: src/components/Footer.astro
      via: import statement
      pattern: import Footer from '../components/Footer.astro'
---

<objective>
Enhance footer component with multi-column layout and social media icons, and implement mobile navigation menu with hamburger toggle and smooth transitions.

Purpose: Create a polished footer with navigation links, social media icons (Twitter, GitHub, LinkedIn), and copyright information using responsive grid layout. Add mobile menu functionality with Vanilla JS toggle and Tailwind transitions for smooth open/close animation.

Output: Enhanced Footer.astro with 3-column layout, social icons using Lucide, and updated Header.astro with hamburger button, mobile menu dropdown, and Vanilla JS toggle script.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-shared-components/02-CONTEXT.md
@.planning/phases/02-shared-components/02-RESEARCH.md
@src/components/Footer.astro
@src/components/Header.astro
@src/layouts/Layout.astro
@src/styles/design-tokens.css
@package.json

<interfaces>
From src/styles/design-tokens.css:
```css
--color-gray-400: #a1a1aa;  /* Social icon default color */
--color-indigo-600: #4f46e5;  /* Hover color */
--spacing-8: 2rem;
--spacing-12: 3rem;
```

From src/components/Footer.astro (existing):
```astro
const currentYear = new Date().getFullYear();
<footer class="w-full border-t border-gray-200 bg-white">
  <div class="mx-auto max-w-7xl px-4 py-8 sm:px-6 lg:px-8">
    <p class="text-center text-sm text-gray-500">
      © {currentYear} Bostrich. All rights reserved.
    </p>
  </div>
</footer>
```
Container pattern: max-w-7xl px-4 py-8 sm:px-6 lg:px-8

From src/components/Header.astro (after Plan 01):
Will contain navLinks array and currentPath constant from Plan 01.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @lucide/astro package for social icons</name>
  <files>package.json</files>
  <read_first>
    - package.json (current dependencies)
  </read_first>
  <action>
    Install @lucide/astro package version 1.8.0 (per D-15):
    ```bash
    npm install @lucide/astro@1.8.0
    ```
    This adds the package to dependencies in package.json and makes Lucide icons available as Astro components.
  </action>
  <verify>
    <automated>grep -n "@lucide/astro" package.json && npm list @lucide/astro 2>/dev/null | grep -v empty</automated>
  </verify>
  <done>@lucide/astro@1.8.0 is installed and listed in package.json dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Enhance Footer with multi-column layout and social icons</name>
  <files>src/components/Footer.astro</files>
  <read_first>
    - src/components/Footer.astro (current copyright-only implementation)
    - src/styles/design-tokens.css (gray-400 and indigo-600 colors, spacing values)
  </read_first>
  <action>
    Replace entire Footer.astro content with enhanced footer implementing D-10 through D-14:

    1. Update frontmatter to import Lucide icons:
       ```astro
       ---
       import { Twitter, Github, Linkedin } from '@lucide/astro';
       const currentYear = new Date().getFullYear();
       ---
       ```

    2. Update footer structure with multi-column grid (per D-11: grid-cols-1 md:grid-cols-3):
       ```astro
       <footer class="w-full border-t border-gray-200 bg-white">
         <div class="mx-auto max-w-7xl px-4 py-12 sm:px-6 lg:px-8">
           <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
             <!-- Left column: Navigation links (per D-12) -->
             <div>
               <h3 class="font-semibold text-gray-900 mb-4">Navigation</h3>
               <ul class="space-y-2">
                 <li><a href="/" class="text-sm text-gray-600 hover:text-indigo-600 transition">Home</a></li>
                 <li><a href="/products" class="text-sm text-gray-600 hover:text-indigo-600 transition">Products</a></li>
                 <li><a href="/about" class="text-sm text-gray-600 hover:text-indigo-600 transition">About</a></li>
                 <li><a href="/pricing" class="text-sm text-gray-600 hover:text-indigo-600 transition">Pricing</a></li>
                 <li><a href="/contact" class="text-sm text-gray-600 hover:text-indigo-600 transition">Contact</a></li>
               </ul>
             </div>

             <!-- Center column: Social media links (per D-13) -->
             <div>
               <h3 class="font-semibold text-gray-900 mb-4">Connect</h3>
               <div class="flex space-x-4">
                 <a href="#" aria-label="Twitter" class="text-gray-400 hover:text-indigo-600 transition">
                   <Twitter class="w-5 h-5" />
                 </a>
                 <a href="#" aria-label="GitHub" class="text-gray-400 hover:text-indigo-600 transition">
                   <Github class="w-5 h-5" />
                 </a>
                 <a href="#" aria-label="LinkedIn" class="text-gray-400 hover:text-indigo-600 transition">
                   <Linkedin class="w-5 h-5" />
                 </a>
               </div>
             </div>

             <!-- Right column: Copyright and legal links (per D-14) -->
             <div>
               <h3 class="font-semibold text-gray-900 mb-4">Legal</h3>
               <ul class="space-y-2">
                 <li><a href="/privacy" class="text-sm text-gray-600 hover:text-indigo-600 transition">Privacy Policy</a></li>
                 <li><a href="/terms" class="text-sm text-gray-600 hover:text-indigo-600 transition">Terms of Service</a></li>
               </ul>
               <p class="mt-4 text-sm text-gray-500">
                 © {currentYear} Bostrich. All rights reserved.
               </p>
             </div>
           </div>
         </div>
       </footer>
       ```

    3. Preserve existing container pattern: max-w-7xl px-4 py-12 sm:px-6 lg:px-8 (changed from py-8 to py-12)
    4. Use Lucide icons for Twitter, GitHub, LinkedIn (per D-15)
    5. Social icons use text-gray-400 with hover:text-indigo-600
  </action>
  <verify>
    <automated>grep -n "from '@lucide/astro'" src/components/Footer.astro && grep -n "grid-cols-1 md:grid-cols-3" src/components/Footer.astro && grep -n "Twitter\|Github\|Linkedin" src/components/Footer.astro</automated>
  </verify>
  <done>Footer.astro imports Twitter, Github, Linkedin from @lucide/astro, has 3-column grid layout (grid-cols-1 md:grid-cols-3), contains left column with 5 navigation links, center column with 3 social icons (Twitter, GitHub, LinkedIn), right column with 2 legal links and copyright, and uses text-gray-400 hover:text-indigo-600 for icons</done>
</task>

<task type="auto">
  <name>Task 3: Add mobile menu with hamburger toggle and smooth transitions</name>
  <files>src/components/Header.astro</files>
  <read_first>
    - src/components/Header.astro (current implementation with desktop navigation from Plan 01)
    - src/styles/design-tokens.css (gray-400 color for hamburger icon)
  </read_first>
  <action>
    Update Header.astro to add mobile menu functionality implementing D-04 through D-06:

    1. Add mobile menu state in frontmatter:
       ```astro
       let isMenuOpen = false;
       ```

    2. Add hamburger button in header after desktop nav (per D-05: visible on mobile, hidden on desktop):
       ```astro
       <!-- Mobile hamburger button -->
       <button
         id="mobile-menu-button"
         aria-label="Toggle menu"
         aria-expanded={isMenuOpen.toString()}
         class="md:hidden p-2 text-gray-600 hover:text-indigo-600 transition"
       >
         <span class="sr-only">Menu</span>
         <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
           <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
         </svg>
       </button>
       ```

    3. Add mobile menu dropdown after desktop nav (per D-06: smooth transitions, per D-04: Vanilla JS):
       ```astro
       <!-- Mobile menu dropdown -->
       <ul
         class={`md:hidden absolute top-full left-0 right-0 bg-white border-b border-gray-200 shadow-lg transition-all ${
           isMenuOpen ? 'max-h-96 opacity-100' : 'max-h-0 opacity-0 overflow-hidden'
         }`}
       >
         {navLinks.map(link => (
           <li>
             <a
               href={link.href}
               class={`block py-3 px-4 text-sm transition hover:text-indigo-600 ${
                 isActive(link.href) ? 'text-indigo-600 font-semibold' : 'text-gray-600'
               }`}
             >
               {link.label}
             </a>
           </li>
         ))}
       </ul>
       ```

    4. Add Vanilla JS script at bottom of component (per D-04):
       ```astro
       <script>
         const menuButton = document.getElementById('mobile-menu-button');
         menuButton?.addEventListener('click', () => {
           isMenuOpen = !isMenuOpen;
           menuButton.setAttribute('aria-expanded', isMenuOpen.toString());
         });
       </script>
       ```

    5. Add relative positioning to nav parent (to position absolute mobile menu):
       Change `<nav>` opening tag to: `<nav class="relative">`

    6. Apply same active/inactive styling to mobile links as desktop (text-indigo-600 font-semibold vs text-gray-600)
  </action>
  <verify>
    <automated>grep -n "mobile-menu-button" src/components/Header.astro && grep -n "md:hidden.*max-h-96" src/components/Header.astro && grep -n "addEventListener" src/components/Header.astro && grep -n "isMenuOpen = !isMenuOpen" src/components/Header.astro</automated>
  </verify>
  <done>Header.astro contains hamburger button with id="mobile-menu-button" and aria-label="Toggle menu", mobile menu dropdown with md:hidden absolute positioning and transition classes (max-h-96 opacity-100 when open, max-h-0 opacity-0 when closed), Vanilla JS script toggling isMenuOpen state on button click, and aria-expanded attribute updates correctly</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→HTML | Static footer and mobile menu render in browser; no authentication or sensitive data |
| browser→JS | Client-side JavaScript for mobile menu toggle (simple boolean state, no external inputs) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | S (Spoofing) | Social media links | accept | Placeholder links (href="#"), no real authentication or accounts. No spoofing risk. |
| T-02-04 | T (Tampering) | Mobile menu state | accept | Client-side boolean toggle, no server-side state. Tampering only affects local browser UI, no security impact. |
| T-02-05 | I (Information disclosure) | N/A | N/A | No sensitive information in footer or mobile menu. |
| T-02-06 | D (Denial of service) | N/A | N/A | Static site with CDN, no DoS risk from footer or mobile menu. |
| T-02-07 | E (Elevation of privilege) | N/A | N/A | No authentication or authorization in footer or mobile menu. |
</threat_model>

<verification>
Phase verification checks:
1. Footer.astro imports Lucide icons correctly (Twitter, Github, Linkedin)
2. Footer uses 3-column grid on desktop (grid-cols-1 md:grid-cols-3)
3. Social icons use text-gray-400 with hover:text-indigo-600
4. Mobile menu button has proper ARIA attributes (aria-label, aria-expanded)
5. Mobile menu uses smooth transitions (Tailwind transition classes)
6. Vanilla JS script properly toggles isMenuOpen state
7. Mobile menu applies same active/inactive styling as desktop navigation
</verification>

<success_criteria>
1. Footer displays on all pages with 3 columns (navigation, social, legal) on desktop
2. Footer collapses to 1 column on mobile with proper spacing
3. Social media icons (Twitter, GitHub, LinkedIn) display correctly using Lucide icons
4. Social icons hover from gray-400 to indigo-600
5. Hamburger button displays on mobile devices (< md breakpoint)
6. Clicking hamburger button toggles mobile menu open/closed
7. Mobile menu uses smooth transition animation (max-h-0 to max-h-96, opacity 0 to 100)
8. Mobile menu links have same active/inactive styling as desktop
9. ARIA attributes (aria-label, aria-expanded) present for accessibility
10. Container pattern preserved in footer (max-w-7xl, px-4 py-12 sm:px-6 lg:px-8)
</success_criteria>

<output>
After completion, create `.planning/phases/02-shared-components/02-02-SUMMARY.md`
</output>
