---
phase: 03-core-pages
plan: 06
type: execute
wave: 6
depends_on: [03-05]
files_modified: [src/pages/contact.astro]
autonomous: true
requirements: [A11Y-02, A11Y-04]
user_setup: []
gap_closure: true
addresses_gaps:
  - "IN-01: Inline script in contact.astro runs in global scope (already addressed in 03-05 but included for completeness)"
  - "IN-02: Missing ARIA labels on form inputs"

must_haves:
  truths:
    - "Contact form inputs have ARIA labels for screen readers"
    - "Contact form script runs in scoped IIFE to prevent namespace pollution"
  artifacts:
    - path: "src/pages/contact.astro"
      provides: "Accessible form with ARIA labels and scoped JavaScript"
      contains: "aria-required, aria-describedby attributes, IIFE-wrapped script"
      min_lines: 85
  key_links:
    - from: "form inputs"
      to: "screen readers"
      via: "ARIA attributes"
      pattern: "aria-"
    - from: "JavaScript code"
      to: "global scope"
      via: "IIFE wrapper"
      pattern: "(() => {"
---

<objective>
Enhance contact form accessibility and code quality: add ARIA labels to form inputs for screen reader support, ensure JavaScript runs in scoped IIFE (addressed in 03-05 but verified here).

Purpose: Address review info item IN-02 (missing ARIA labels) to improve accessibility. IN-01 (global scope script) was already addressed in Plan 03-05, but this plan verifies the fix is complete and consistent.

Output: Contact form is more accessible with proper ARIA attributes for screen readers, JavaScript runs in scoped IIFE preventing namespace pollution.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-core-pages/03-REVIEW.md
@.planning/phases/03-core-pages/03-CONTEXT.md
@.planning/phases/03-core-pages/03-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ARIA labels to contact form inputs</name>
  <files>src/pages/contact.astro</files>
  <behavior>
    - Email input has aria-required="true" attribute
    - Email input has aria-describedby="email-help" attribute
    - Email help text has id="email-help" matching aria-describedby
    - All required inputs have aria-required="true"
    - Screen readers can read form labels and help text properly
  </behavior>
  <action>
Update src/pages/contact.astro form inputs to add ARIA attributes:

1. Update name input (already has label, just add ARIA):
```astro
<input
  type="text"
  id="name"
  name="name"
  class="w-full px-4 py-3 border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-600"
  required
  aria-required="true"
/>
```

2. Update email input with help text and ARIA:
```astro
<label for="email" class="block text-sm font-medium text-gray-900 mb-2">Email</label>
<input
  type="email"
  id="email"
  name="email"
  class="w-full px-4 py-3 border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-600"
  required
  aria-required="true"
  aria-describedby="email-help"
/>
<p id="email-help" class="text-sm text-gray-500 mt-1">We'll never share your email with anyone else.</p>
```

3. Update company input (optional field, no aria-required):
```astro
<input
  type="text"
  id="company"
  name="company"
  class="w-full px-4 py-3 border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-600"
/>
```

4. Update message input:
```astro
<textarea
  id="message"
  name="message"
  rows="5"
  class="w-full px-4 py-3 border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-600"
  required
  aria-required="true"
></textarea>
```

Note: The IIFE script wrapper was already added in Plan 03-05 Task 2, so this task focuses only on ARIA accessibility improvements.

This addresses review info item IN-02 (missing ARIA labels on form inputs).
</action>
  <verify>
    <automated>grep -q "aria-required=\"true\"" src/pages/contact.astro && grep -q "aria-describedby=\"email-help\"" src/pages/contact.astro && grep -q "id=\"email-help\"" src/pages/contact.astro && npm run build 2>&1 | grep -q "dist/contact/index.html"</automated>
  </verify>
  <done>Contact form inputs have proper ARIA labels (aria-required, aria-describedby) with matching help text IDs. Screen readers can now properly announce form fields and help text.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→form | User input crosses here (same as Plan 03-05) |
| screen reader→ARIA | Assistive technology reads ARIA attributes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | T (Tampering) | ARIA attributes | accept | ARIA attributes are declarative metadata for screen readers. No security implications. |
| T-03-06-02 | I (Information disclosure) | email-help text | accept | Help text is visible on page ("We'll never share your email"), no sensitive information disclosed. |
</threat_model>

<verification>
Build verification:
- `npm run build` completes successfully
- Contact page HTML includes ARIA attributes on form inputs
- aria-required attributes present on required fields
- aria-describedby and matching help text ID present on email field

Accessibility verification:
- All required inputs have aria-required="true"
- Email field has aria-describedby="email-help"
- Help text paragraph has id="email-help"
- Labels properly associate with inputs via for/id attributes

Code quality verification:
- Script is wrapped in IIFE (already done in 03-05, verify still present)
- No global variable pollution from contact form script
</verification>

<success_criteria>
1. Contact form HTML includes aria-required="true" on all required inputs
2. Email input has aria-describedby="email-help" attribute
3. Email help text paragraph has id="email-help" matching aria-describedby
4. Contact form script is wrapped in IIFE (verify from Plan 03-05)
5. Build completes successfully with no errors
6. Generated HTML contains all ARIA attributes in correct locations
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-pages/03-06-SUMMARY.md`
</output>
