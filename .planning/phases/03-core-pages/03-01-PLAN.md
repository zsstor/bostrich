---
phase: 03
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/pages/index.astro]
autonomous: true
requirements: [PAGE-01, PAGE-02]
user_setup: []

must_haves:
  truths:
    - "User sees home page with hero section on load"
    - "Hero section displays headline, subheadline, and two CTAs"
    - "Featured products section shows 3-4 products in grid layout"
    - "All CTAs link to correct pages (Get Started → /products, Learn More → /about)"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Home page with hero and featured products"
      min_lines: 50
  key_links:
    - from: "src/pages/index.astro"
      to: "/products"
      via: "Get Started CTA button"
      pattern: "href.*\/products"
    - from: "src/pages/index.astro"
      to: "/about"
      via: "Learn More CTA button"
      pattern: "href.*\/about"
    - from: "src/pages/index.astro"
      to: "src/lib/data.ts"
      via: "import products from data"
      pattern: "import.*getAllProducts"
---

<objective>
Create home page with hero section and featured products grid

Purpose: Establish main landing page with SaaS-style hero and product showcase, providing clear entry points to the site
Output: Complete home page (index.astro) with hero section and featured products grid
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-pages/03-CONTEXT.md
@.planning/phases/03-core-pages/03-UI-SPEC.md
@src/lib/data.ts
@src/layouts/Layout.astro
@src/styles/design-tokens.css
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create hero section with headline, subheadline, and CTAs</name>
  <files>src/pages/index.astro</files>
  <read_first>
    - src/pages/index.astro (existing minimal page)
    - src/layouts/Layout.astro (layout import pattern)
    - .planning/phases/03-core-pages/03-CONTEXT.md (specific ideas for hero content)
    - .planning/phases/03-core-pages/03-UI-SPEC.md (typography and color tokens)
  </read_first>
  <action>
    1. Import Layout component: `import Layout from '../layouts/Layout.astro';`
    2. Create hero section with single-column centered layout:
       - Use container pattern: `<section class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16 md:py-24">`
       - Hero headline: `<h1 class="text-4xl md:text-6xl font-extrabold text-gray-900 text-center mb-4">Scalable Solutions to Take Your Business to the Next Level</h1>`
         - Use display font (64px) for mobile, scaled down to text-6xl for larger screens
       - Hero subheadline: `<p class="text-lg text-gray-600 text-center mb-8 max-w-2xl mx-auto">Industry-leading vertical mobility and offboarding accelerators for modern enterprises</p>`
         - Use body font (16px) with 1.5 line-height
       - Primary CTA: `<a href="/products" class="inline-block bg-indigo-600 text-white font-bold py-3 px-6 rounded-lg hover:bg-indigo-700 transition-colors">Get Started</a>`
         - Use accent color (indigo-600) per UI-SPEC
         - 48px height for touch targets
       - Secondary CTA: `<a href="/about" class="inline-block text-indigo-600 font-semibold py-3 px-6 hover:text-indigo-700 transition-colors">Learn More</a>`
         - Use accent color with semantic text, not background
    3. Wrap hero in Layout component: `<Layout title="Bostrich — Scalable Solutions to Take Your Business to the Next Level" description="Industry-leading vertical mobility solutions and offboarding accelerators for modern enterprises. Take your business to new heights with Bostrich.">` (page 03-UI-SPEC for titles and meta descriptions)
    4. Use semantic HTML: `<main>` wrapper for page content
  </action>
  <verify>
    <automated>grep -q "text-6xl font-extrabold" src/pages/index.astro && grep -q "href=\"/products\"" src/pages/index.astro && grep -q "href=\"/about\"" src/pages/index.astro</automated>
  </verify>
  <done>Hero section displays with headline "Scalable Solutions to Take Your Business to the Next Level", subheadline "Industry-leading vertical mobility and offboarding accelerators for modern enterprises", primary CTA "Get Started" linking to /products, secondary CTA "Learn More" linking to /about</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add featured products section with grid layout</name>
  <files>src/pages/index.astro</files>
  <read_first>
    - src/pages/index.astro (hero section from Task 1)
    - src/lib/data.ts (product data structure)
    - .planning/phases/03-core-pages/03-CONTEXT.md (featured products decisions)
    - .planning/phases/03-core-pages/03-UI-SPEC.md (spacing tokens)
  </read_first>
  <action>
    1. Import product data: `import { getAllProducts } from '../lib/data.ts';`
    2. Get first 4 products: `const featuredProducts = getAllProducts().slice(0, 4);` (per CONTEXT.md D-02)
    3. Create featured products section below hero:
       - Section heading: `<h2 class="text-3xl font-bold text-gray-900 text-center mb-12">Featured Products</h2>`
         - Use heading size (24px) with bold weight (700)
       - Grid layout: `<div class="grid grid-cols-1 md:grid-cols-3 gap-8">` (3 columns on desktop per UI-SPEC exceptions)
       - Product cards (for each product in featuredProducts):
         * Card container: `<div class="bg-white border border-gray-200 rounded-lg p-6 hover:shadow-md transition-shadow">`
         * Product name: `<h3 class="text-xl font-semibold text-gray-900 mb-2">${product.name}</h3>` (24px with semibold weight 600)
         * Product tagline: `<p class="text-indigo-600 font-medium mb-3">${product.tagline}</p>` (accent color)
         * Product description: `<p class="text-gray-600">${product.description}</p>` (secondary text color)
    4. Use spacing tokens: gap-8 (32px) between cards, p-6 (24px) for card padding
    5. Section padding: `<section class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16 md:py-24">`
  </action>
  <verify>
    <automated>grep -q "getAllProducts" src/pages/index.astro && grep -q "Featured Products" src/pages/index.astro && grep -q "grid-cols-1 md:grid-cols-3" src/pages/index.astro</automated>
  </verify>
  <done>Featured products section displays heading "Featured Products" and 4 products in 3-column grid layout on desktop, with each card showing product name, tagline, and description</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Hero CTAs | Untrusted input crosses from user clicks to navigation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | I (Integrity) | Navigation links in hero CTAs | accept | Static links to known routes (/products, /about), no user input involved |
| T-03-02 | T (Tampering) | Product data rendering | accept | Static data from src/lib/data.ts, no database or API involved |

</threat_model>

<verification>
1. Build succeeds: `npm run build` completes without errors
2. Hero section displays correctly on home page
3. Both CTAs link to correct pages
4. Featured products grid shows 4 products
5. Page is responsive on mobile (1 column) and desktop (3 columns)
6. Page title and meta description are set correctly
</verification>

<success_criteria>
1. Home page renders with hero section displaying headline, subheadline, and two CTAs
2. Hero CTAs link to /products (Get Started) and /about (Learn More)
3. Featured products section displays 4 products in 3-column grid on desktop
4. Product cards show name, tagline, and description using design tokens
5. Page uses semantic HTML (main, section, h1, h2, h3)
6. Page is responsive (1 column on mobile, 3 columns on desktop)
7. Page title and meta description match SEO requirements
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-pages/03-01-SUMMARY.md`
</output>
