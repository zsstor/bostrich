---
phase: 03
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [src/pages/pricing.astro]
autonomous: true
requirements: [PAGE-04, PAGE-05]
user_setup: []

must_haves:
  truths:
    - "User sees Pricing page with 7 product sections"
    - "Each product section has pricing table with 4 tiers"
    - "Business tier is highlighted as 'Recommended'"
    - "Features are displayed as full feature list per tier"
    - "Pricing tables show price (e.g., '3 rungs') and feature checkmarks"
  artifacts:
    - path: "src/pages/pricing.astro"
      provides: "Pricing page with tiered subscriptions for all products"
      min_lines: 100
  key_links:
    - from: "src/pages/pricing.astro"
      to: "src/lib/data.ts"
      via: "import products from data"
      pattern: "import.*getAllProducts"
---

<objective>
Create Pricing page with tiered subscriptions for all 7 products

Purpose: Display comprehensive pricing tables with Free/Basic/Business/Enterprise tiers for each product, highlighting Business tier as recommended, using SaaS-style layout
Output: Complete Pricing page (pricing.astro) with 7 product sections and pricing tables
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-pages/03-CONTEXT.md
@.planning/phases/03-core-pages/03-UI-SPEC.md
@src/lib/data.ts
@src/layouts/Layout.astro
@src/styles/design-tokens.css
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create Pricing page with product sections and pricing tables</name>
  <files>src/pages/pricing.astro</files>
  <read_first>
    - src/lib/data.ts (product pricing and feature data structure)
    - src/layouts/Layout.astro (layout import pattern)
    - .planning/phases/03-core-pages/03-CONTEXT.md (pricing decisions)
    - .planning/phases/03-core-pages/03-UI-SPEC.md (spacing and color tokens)
  </read_first>
  <action>
    1. Import Layout and product data:
       ```astro
       import Layout from '../layouts/Layout.astro';
       import { getAllProducts } from '../lib/data.ts';
       ```
    2. Get all products: `const products = getAllProducts();`
    3. Create Pricing page structure:
       - Use container pattern: `<main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16 md:py-24">`
       - Page heading: `<h1 class="text-4xl md:text-6xl font-bold text-gray-900 mb-16 text-center">Pricing</h1>` (24px bold)
    4. Create product sections (for each product in products):
       - Section wrapper: `<section class="mb-16">` (64px spacing between products per 3xl token)
       - Product heading: `<h2 class="text-2xl font-bold text-gray-900 mb-4">${product.name}</h2>` (24px bold)
       - Product tagline: `<p class="text-lg text-gray-600 mb-8">${product.tagline}</p>` (18px)
       - Pricing table layout: `<div class="grid grid-cols-1 md:grid-cols-4 gap-6">` (4 columns = 4 tiers)
       - Pricing tier cards (for each tier in product.pricing object):
         * Tier card container: `<div class="border border-gray-200 rounded-lg p-6 flex flex-col">`
         * If tier is "Business" (recommended per CONTEXT.md D-09):
           - Add recommended badge: `<div class="bg-indigo-600 text-white text-xs font-semibold py-1 px-3 rounded-full mb-4 inline-block">Recommended</div>`
           - Add visual emphasis: `border-2 border-indigo-600` (thicker border with accent color)
         * Tier name: `<h3 class="text-xl font-bold text-gray-900 mb-2">${tierName}</h3>` (24px semibold)
         * Tier price: `<p class="text-3xl font-extrabold text-gray-900 mb-4">${price}</p>` (64px extrabold)
         * Feature list:
           - `<ul class="space-y-3 flex-1">` (12px spacing between features per sm token)
           - For each feature in product.features:
             * Feature item: `<li class="flex items-center text-gray-600"><span class="text-indigo-600 mr-2">✓</span>${feature}</li>`
               * Use accent color (indigo-600) for checkmarks per UI-SPEC
    5. Wrap in Layout: `<Layout title="Pricing — Bostrich Enterprise Solutions" description="Tiered pricing for vertical mobility, offboarding accelerators, and growth infrastructure. Choose a plan that fits your enterprise needs.">`
    6. Use semantic HTML: `<main>`, `<section>`, `<h2>`, `<h3>`, `<ul>`, `<li>`
  </action>
  <verify>
    <automated>grep -q "getAllProducts" src/pages/pricing.astro && grep -q "Recommended" src/pages/pricing.astro && grep -q "grid-cols-1 md:grid-cols-4" src/pages/pricing.astro</automated>
  </verify>
  <done>Pricing page displays 7 product sections, each with pricing table showing 4 tiers (Free/Basic/Business/Enterprise), Business tier highlighted with "Recommended" badge and thicker border, all features displayed with checkmarks</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Pricing page | No user input, read-only page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | N/A | Pricing page content | accept | Static content only, no user input or data processing |

</threat_model>

<verification>
1. Build succeeds: `npm run build` completes without errors
2. Pricing page displays 7 product sections
3. Each product section has 4-tier pricing table
4. Business tier is highlighted with "Recommended" badge and visual emphasis
5. All features are displayed with checkmarks
6. Tier prices are displayed correctly (e.g., "3 rungs", "15ft ramp")
7. Page is responsive (1 column on mobile, 4 columns on desktop)
8. Page title and meta description are set correctly
</verification>

<success_criteria>
1. Pricing page displays 7 product sections (one per product)
2. Each product section shows 4-tier pricing table (Free, Basic, Business, Enterprise)
3. Business tier is highlighted with "Recommended" badge and thicker indigo border
4. All tiers display price (e.g., "3 rungs", "15ft ramp")
5. All features from product data are displayed with indigo checkmarks
6. Page uses semantic HTML (main, section, h2, h3, ul, li)
7. Page title and meta description match SEO requirements
8. Page is responsive (1 column on mobile, 4 columns on desktop)
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-pages/03-03-SUMMARY.md`
</output>
