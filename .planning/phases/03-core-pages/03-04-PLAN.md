---
phase: 03
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [src/pages/contact.astro]
autonomous: true
requirements: [PAGE-06]
user_setup: []

must_haves:
  truths:
    - "User sees Contact page with enterprise inquiry form"
    - "Form has 4 fields (Name, Email, Company, Message)"
    - "Form submission shows success message and prevents default behavior"
    - "Success message replaces form after submission"
  artifacts:
    - path: "src/pages/contact.astro"
      provides: "Contact page with enterprise inquiry form"
      min_lines: 60
  key_links:
    - from: "src/pages/contact.astro"
      to: "Form submission handler"
      via: "JavaScript prevent-default"
      pattern: "event.preventDefault\(\)"
---

<objective>
Create Contact page with enterprise inquiry form and success message

Purpose: Provide enterprise inquiry form with 4 fields, using JavaScript prevent-default for static form submission and displaying success message, reinforcing SaaS enterprise theme
Output: Complete Contact page (contact.astro) with form and success message
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-pages/03-CONTEXT.md
@.planning/phases/03-core-pages/03-UI-SPEC.md
@src/layouts/Layout.astro
@src/styles/design-tokens.css
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create Contact page with enterprise form and success message</name>
  <files>src/pages/contact.astro</files>
  <read_first>
    - src/layouts/Layout.astro (layout import pattern)
    - .planning/phases/03-core-pages/03-CONTEXT.md (contact form decisions)
    - .planning/phases/03-core-pages/03-UI-SPEC.md (spacing, color, and typography tokens)
  </read_first>
  <action>
    1. Import Layout component: `import Layout from '../layouts/Layout.astro';`
    2. Create Contact page structure:
       - Use container pattern: `<main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16 md:py-24">`
       - Page heading: `<h1 class="text-4xl md:text-6xl font-bold text-gray-900 mb-4 text-center">Contact</h1>` (24px bold)
       - Form section heading: `<h2 class="text-2xl font-bold text-gray-900 mb-8 text-center">Enterprise Inquiries</h2>` (24px bold)
    3. Create form with 4 fields:
       - Form element: `<form id="contact-form" class="max-w-2xl mx-auto space-y-6">`
       - Name field:
         * Label: `<label for="name" class="block text-sm font-medium text-gray-900 mb-2">Name</label>` (14px medium)
         * Input: `<input type="text" id="name" name="name" class="w-full px-4 py-3 border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-600" required>`
           * Use 48px height for touch targets (A11Y-05)
           * Use accent color (indigo-600) for focus ring per UI-SPEC
       - Email field:
         * Label: `<label for="email" class="block text-sm font-medium text-gray-900 mb-2">Email</label>`
         * Input: `<input type="email" id="email" name="email" class="w-full px-4 py-3 border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-600" required>`
       - Company field:
         * Label: `<label for="company" class="block text-sm font-medium text-gray-900 mb-2">Company</label>`
         * Input: `<input type="text" id="company" name="company" class="w-full px-4 py-3 border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-600">` (optional)
       - Message field:
         * Label: `<label for="message" class="block text-sm font-medium text-gray-900 mb-2">Message</label>`
         * Textarea: `<textarea id="message" name="message" rows="5" class="w-full px-4 py-3 border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-600" required>`
       - Submit button: `<button type="submit" class="w-full bg-indigo-600 text-white font-bold py-3 px-6 rounded-lg hover:bg-indigo-700 transition-colors">Submit Inquiry</button>`
         * Use accent color (indigo-600) per UI-SPEC
         * Use 48px height for touch targets
    4. Create success message (hidden by default):
       ```astro
       <div id="success-message" class="hidden max-w-2xl mx-auto bg-white border border-gray-200 rounded-lg p-8 text-center">
         <h3 class="text-2xl font-bold text-gray-900 mb-4">Thank you for your inquiry.</h3>
         <p class="text-lg text-gray-600">Our enterprise team will respond within 1-2 business days.</p>
       </div>
       ```
    5. Add JavaScript for form submission (using Vanilla JS per Phase 2 D-04):
       ```astro
       <script>
         const form = document.getElementById('contact-form');
         const successMessage = document.getElementById('success-message');
         
         form.addEventListener('submit', (event) => {
           event.preventDefault();
           form.classList.add('hidden');
           successMessage.classList.remove('hidden');
         });
       </script>
       ```
       - Use `classList.remove/add` for static build compatibility
    6. Wrap in Layout: `<Layout title="Contact — Bostrich Enterprise Inquiries" description="Enterprise inquiries and partnership opportunities. Contact our team to discuss scalable solutions for your organization.">`
    7. Use semantic HTML: `<main>`, `<h1>`, `<h2>`, `<form>`, `<label>`, `<input>`, `<textarea>`, `<button>`, `<div>`
  </action>
  <verify>
    <automated>grep -q "Enterprise Inquiries" src/pages/contact.astro && grep -q "contact-form" src/pages/contact.astro && grep -q "event.preventDefault()" src/pages/contact.astro && grep -q "success-message" src/pages/contact.astro</automated>
  </verify>
  <done>Contact page displays enterprise inquiry form with 4 fields (Name, Email, Company, Message), submit button "Submit Inquiry", success message "Thank you for your inquiry. Our enterprise team will respond within 1-2 business days." that replaces form on submission</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Form input | Untrusted input from user (name, email, company, message) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | I (Integrity) | Form submission | accept | Client-side only, prevent-default with no server submission. No data persistence or API calls. |
| T-03-06 | X (XSS) | Form input fields | accept | No server-side processing, client-side prevent-default only. Input not reflected to page, stored, or transmitted. |

</threat_model>

<verification>
1. Build succeeds: `npm run build` completes without errors
2. Contact page displays form with 4 fields
3. All fields have proper labels and semantic structure
4. Form submission prevents default behavior (JavaScript prevent-default)
5. Success message replaces form after submission
6. All interactive elements use semantic HTML
7. Page title and meta description are set correctly
</verification>

<success_criteria>
1. Contact page displays heading "Enterprise Inquiries" and 4-field form
2. Form fields include Name, Email, Company (optional), Message
3. All fields have proper labels and semantic HTML (label, input, textarea)
4. Submit button says "Submit Inquiry" and uses accent color (indigo-600)
5. JavaScript prevent-default prevents page reload on form submission
6. Success message replaces form after submission
7. Success message says "Thank you for your inquiry. Our enterprise team will respond within 1-2 business days."
8. Page uses semantic HTML (main, h1, h2, form, label, input, textarea, button)
9. Page title and meta description match SEO requirements
10. Page is responsive (container with max-width constraints)
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-pages/03-04-SUMMARY.md`
</output>
