---
phase: 01
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, astro.config.mjs, tsconfig.json, tsconfig.node.json, src/pages/index.astro]
autonomous: true
requirements: [FND-01]
user_setup: []

must_haves:
  truths:
    - Astro project initializes successfully
    - Project builds static output without errors
    - Project structure follows Astro conventions (src/pages/, src/layouts/, src/components/)
    - TypeScript configuration is enabled
    - Static output mode is configured in astro.config.mjs
  artifacts:
    - path: package.json
      provides: Project dependencies and scripts
      contains: astro@6.1.7
    - path: astro.config.mjs
      provides: Astro configuration
      contains: output: 'static'
    - path: tsconfig.json
      provides: TypeScript configuration
      contains: extends: astro/tsconfigs/strict
  key_links:
    - from: astro.config.mjs
      to: src/pages/
      via: build process
      pattern: output.*static
    - from: package.json
      to: astro.config.mjs
      via: astro build command
      pattern: build.*astro
---

<objective>
Initialize Astro v6.1.7 project with static output configuration

Purpose: Establish the foundation for the Bostrich static SaaS parody site using Astro's modern static site generation capabilities
Output: Working Astro project configured for static export with TypeScript enabled
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-design-system/01-CONTEXT.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Astro project from minimal template</name>
  <files>package.json, astro.config.mjs, tsconfig.json, tsconfig.node.json, src/pages/index.astro</files>
  <action>
    Per D-05, start from Astro's minimal template.

    Run initialization command:
    ```bash
    npm create astro@latest . -- --template minimal --yes --no-install --no-git
    ```

    Then install dependencies:
    ```bash
    npm install --silent
    ```

    Verify Astro version is 6.1.7 by checking package.json. If not, install specific version:
    ```bash
    npm install astro@6.1.7 --save-exact
    ```

    Ensure astro.config.mjs has static output configured:
    ```javascript
    export default defineConfig({
      output: 'static',
    });
    ```

    Remove any default files we don't need (readme, example files).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Astro project initialized with v6.1.7, builds static output successfully</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build system | npm install introduces external dependencies from npm registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | npm install | accept | Using official npm registry, package-lock.json locks dependencies |
| T-01-02 | Tampering | npm packages | accept | npm install uses npm registry integrity checks, package-lock.json ensures reproducible builds |
</threat_model>

<verification>
Build succeeds without errors, static output generated in dist/ directory
</verification>

<success_criteria>
Astro project initializes and builds static output without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-system/01-01-SUMMARY.md`
</output>
