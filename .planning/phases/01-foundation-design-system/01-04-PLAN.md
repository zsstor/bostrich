---
phase: 01
plan: 04
type: execute
wave: 4
depends_on: [01-03]
files_modified: [src/layouts/Layout.astro]
autonomous: true
requirements: [FND-06]
user_setup: []

must_haves:
  truths:
    - Viewport meta tag configured in Layout
    - Site displays correctly on mobile devices
    - No horizontal scrolling on mobile
    - Content scales appropriately on different screen sizes
  artifacts:
    - path: src/layouts/Layout.astro
      provides: Viewport meta configuration
      contains: viewport meta tag
  key_links:
    - from: src/layouts/Layout.astro
      to: browsers
      via: viewport meta tag
      pattern: name="viewport"
---

<objective>
Configure viewport meta tag for responsive design

Purpose: Ensure the site displays correctly on mobile, tablet, and desktop devices by configuring the viewport meta tag
Output: Viewport meta tag configured and site displays correctly on mobile devices
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-design-system/01-CONTEXT.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure viewport meta tag for responsive design</name>
  <files>src/layouts/Layout.astro</files>
  <action>
    Update src/layouts/Layout.astro to configure viewport meta tag for responsive design.

    The Layout.astro created in 01-03 already includes a viewport meta tag. Verify and ensure it's properly configured:

    ```astro
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    ```

    This ensures:
    - Width is set to device width (not desktop width)
    - Initial scale is 1.0 (no zoom)
    - Site renders correctly on mobile devices
    - No horizontal scrolling on mobile
    - Content scales appropriately across screen sizes

    If the viewport meta tag is missing or incomplete, update it to match the above.

    Additionally, per D-07, ensure font loading strategy is configured by adding to the head:
    ```astro
    <style>
      @font-face {
        font-family: 'Inter';
        font-display: swap;
        src: local('Inter'), local('Inter-Regular');
      }
      body {
        font-family: 'Inter', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      }
    </style>
    ```
  </action>
  <verify>
    <automated>grep 'name="viewport"' src/layouts/Layout.astro | grep -q "width=device-width"</automated>
  </verify>
  <done>Viewport meta tag configured and site displays correctly on mobile devices</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Pure HTML configuration, no external inputs |

## STRIDE Threat Register

No threats identified - viewport meta tag is a standard HTML configuration.
</threat_model>

<verification>
Viewport meta tag exists with proper responsive configuration, mobile testing shows correct display
</verification>

<success_criteria>
Viewport meta tag configured and site displays correctly on mobile devices
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-system/01-04-SUMMARY.md`
</output>
