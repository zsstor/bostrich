---
phase: 04-individual-product-pages
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: [src/lib/data.ts, src/pages/products/[slug].astro]
autonomous: true
requirements: [PROD-03, PROD-04, PROD-05, PROD-06, PROD-07, PROD-08]
user_setup: []

must_haves:
  truths:
    - "Individual product pages exist for all 7 products (ladder, agile-ladder, ramp, slide, thin-slide, cliff, scaffolding)"
    - "Dynamic routing with getStaticPaths() generates static pages for all products"
    - "Product pages include SaaS-style hero section with product-specific value proposition"
    - "Product pages include features list with technical buzzwords"
    - "Product pages display high-quality product imagery"
    - "Product pages include pricing tiers specific to that product"
  artifacts:
    - path: "src/lib/data.ts"
      provides: "Product interface with imageUrl field and updated product data"
      contains: "imageUrl"
    - path: "src/pages/products/[slug].astro"
      provides: "Dynamic product page with hero, features, and pricing"
      exports: ["getStaticPaths"]
  key_links:
    - from: "src/pages/products/[slug].astro"
      to: "src/lib/data.ts"
      via: "getProductBySlug() function"
      pattern: "getProductBySlug\\(\\)"
    - from: "getStaticPaths()"
      to: "Static HTML generation"
      via: "Astro build process"
      pattern: "getStaticPaths"
---

<objective>
Create dynamic product pages for all 7 products using Astro's getStaticPaths() for static generation. Each page includes hero section, features list, product imagery, and pricing tiers.

Purpose: Provide comprehensive product landing pages that mimic SaaS product pages with full feature descriptions and tiered pricing.

Output: Dynamic product page route (src/pages/products/[slug].astro) with hero section, features list, imagery, and pricing tiers for all 7 products.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-individual-product-pages/04-UI-SPEC.md

# Product data structure from src/lib/data.ts (current)
```typescript
export interface Product {
  id: string;
  name: string;
  slug: string;
  tagline: string;
  description: string;
  features: string[];
  pricing?: {
    free?: string;
    basic?: string;
    business?: string;
    enterprise?: string;
  };
}

export function getProductBySlug(slug: string): Product | undefined
export function getAllProducts(): Product[]
```

# Pricing table pattern from src/pages/pricing.astro (Phase 3, Plan 03-03)
- Uses Object.entries(product.pricing || {}) to iterate over pricing tiers
- Grid layout: grid-cols-1 md:grid-cols-4 gap-6
- Business tier highlighting with "Recommended" badge and thicker border
- Features list with indigo checkmarks

# Layout.astro from src/layouts/Layout.astro (Phase 1, Plan 01-03)
- Accepts title and description props
- Provides Header and Footer components
- Container pattern: max-w-7xl mx-auto px-4 sm:px-6 lg:px-8

# Astro dynamic routing pattern
- Filename: [slug].astro creates dynamic route parameter
- getStaticPaths() returns array of { params: { slug: string } }
- Props interface includes slug parameter
- Astro.url.pathname provides current path
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add image URLs to product data structure</name>
  <files>src/lib/data.ts</files>
  <action>Update src/lib/data.ts to add image URLs for all products:

1. Add imageUrl field to Product interface:
   ```typescript
   export interface Product {
     id: string;
     name: string;
     slug: string;
     tagline: string;
     description: string;
     features: string[];
     pricing?: {
       free?: string;
       basic?: string;
       business?: string;
       enterprise?: string;
     };
     imageUrl?: string;  // Add this field
   }
   ```

2. Add imageUrl property to all 7 products with Unsplash placeholder images:
   - ladder: "https://images.unsplash.com/photo-1597383822240-9059df348774?w=800&q=80"
   - agile-ladder: "https://images.unsplash.com/photo-1504307651254-35680f356dfd?w=800&q=80"
   - ramp: "https://images.unsplash.com/photo-1503387762-592deb58ef4e?w=800&q=80"
   - slide: "https://images.unsplash.com/photo-1590416997829-71ed96d808c0?w=800&q=80"
   - thin-slide: "https://images.unsplash.com/photo-1571019613454-1cb2f99b2d8b?w=800&q=80"
   - cliff: "https://images.unsplash.com/photo-1454496522488-7a8e488e8606?w=800&q=80"
   - scaffolding: "https://images.unsplash.com/photo-1504307651254-35680f356dfd?w=800&q=80"

3. Use alt text for accessibility (each image has descriptive alt text in the next task)

4. Verify TypeScript compilation succeeds</action>
  <verify>npm run build succeeds and src/lib/data.ts includes imageUrl field for all 7 products</verify>
  <done>Product interface updated with imageUrl field and all products have image URLs</done>
</task>

<task type="auto">
  <name>Task 2: Create dynamic product page route with getStaticPaths()</name>
  <files>src/pages/products/[slug].astro</files>
  <action>Create src/pages/products/[slug].astro with the following structure:

1. Import Layout from '../../layouts/Layout.astro', { getAllProducts, getProductBySlug } from '../../lib/data.ts'
2. Define getStaticPaths() to generate static paths for all products:
   ```typescript
   export async function getStaticPaths() {
     const products = getAllProducts();
     return products.map((product) => ({
       params: { slug: product.slug },
       props: { product },
     }));
   }
   ```

3. Define Astro interface for props:
   ```typescript
   interface Props {
     product: Product;
   }
   const { product } = Astro.props;
   ```

4. Handle 404 for invalid slug: if (!product) return Astro.redirect('/404')

5. Render Layout component with dynamic title and description based on product data

6. Inside main section (max-w-7xl container, py-16 md:py-24):

   **Hero Section:**
   - Container: grid grid-cols-1 lg:grid-cols-2 gap-12 items-center
   - Hero image: img tag with src={product.imageUrl}, alt={`${product.name} - ${product.tagline}`, width="800", height="600", class="w-full h-96 object-cover rounded-lg mb-6 lg:mb-0"
   - Hero content:
     - h1: product.name (text-4xl md:text-5xl font-bold, mb-4)
     - p tagline: product.tagline (text-2xl text-indigo-600 font-medium, mb-6)
     - p description: product.description (text-lg text-gray-600 leading-relaxed, mb-8)
     - a CTA: "Get Started" (bg-indigo-600 text-white px-8 py-3 rounded-lg font-medium hover:bg-indigo-700, href="/pricing")

   **Features Section:**
   - Container: max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16 md:py-24
   - h2 heading: "Features" (text-3xl md:text-4xl font-bold, centered, mb-12)
   - Features grid: grid grid-cols-1 md:grid-cols-2 gap-6, max-w-4xl mx-auto
   - Feature items: map over product.features, each with ✓ checkmark (text-indigo-600 mr-3 text-2xl) and feature text (text-lg text-gray-700 font-medium)

   **Pricing Section:**
   - Container: max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16 md:py-24
   - h2 heading: "Pricing" (text-3xl md:text-4xl font-bold, centered, mb-12)
   - Pricing grid: grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6
   - Pricing cards: map over Object.entries(product.pricing || {}), each card with:
     - Border: border border-gray-200 rounded-lg p-6 flex flex-col (border-2 border-indigo-600 for business tier)
     - "Recommended" badge: bg-indigo-600 text-white text-xs font-semibold py-1 px-3 rounded-full mb-4 inline-block (only for business tier)
     - h3 tier name: text-xl font-bold text-gray-900 mb-2 capitalize
     - p price: text-3xl font-extrabold text-gray-900 mb-4
     - Features list: ul space-y-3 flex-1, each li with ✓ checkmark (text-indigo-600 mr-2) and feature text (text-gray-600)
     - CTA button: a tag with w-full bg-indigo-600 text-white py-2 rounded-lg font-medium hover:bg-indigo-700 (or bg-white text-indigo-600 border border-indigo-600 for enterprise tier)

7. Ensure semantic HTML: main, section, h1, h2, h3, img, ul, li, a elements
8. Use design tokens for colors and spacing
9. Verify responsive layout (stacked on mobile, 2-column on tablet, 4-column on desktop)
10. Add loading="lazy" to hero image (but not fetchpriority="high" since it's not LCP candidate on product pages)</action>
  <verify>npm run build succeeds and generates 7 static HTML files for /products/ladder, /products/agile-ladder, /products/ramp, /products/slide, /products/thin-slide, /products/cliff, /products/scaffolding</verify>
  <done>Dynamic product pages exist for all 7 products with hero section, features list, imagery, and pricing tiers</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → Product slug parameter | Slug in URL path controls which product data is fetched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Spoofing | Product slug parameter | mitigate | getStaticPaths() only generates valid slugs from product data array, preventing arbitrary slug injection |
| T-04-07 | Tampering | Product data array | accept | Static data at build time, no user modification possible |
| T-04-08 | Information disclosure | Product details | accept | All product data is public - no sensitive information exposed |
| T-04-09 | Denial of service | Page load performance | mitigate | Static site generation, cached CDN delivery, pre-rendered at build time |
| T-04-10 | Elevation of privilege | Product slug traversal | mitigate | getProductBySlug() only returns product if slug exists in array, otherwise 404 redirect |
</threat_model>

<verification>
- All 7 product pages generate successfully at build time
- getStaticPaths() returns array of 7 slugs
- Each product page displays hero section with product name, tagline, and description
- Hero images load correctly from Unsplash URLs
- Features section displays all product features with checkmarks
- Pricing section displays 4 tiers (Free, Basic, Business, Enterprise)
- Business tier has "Recommended" badge and thicker border
- Responsive layout works (stacked mobile, 2-column tablet, 4-column desktop)
- Page titles and meta descriptions are product-specific
- Build succeeds without errors
- Generated HTML files exist for all 7 product slugs
</verification>

<success_criteria>
- All 7 product pages exist and are statically generated
- Dynamic routing with getStaticPaths() works correctly
- Hero section displays product-specific value proposition
- Features list shows technical buzzwords with checkmarks
- Product images display correctly from Unsplash URLs
- Pricing tiers match product data (Free, Basic, Business, Enterprise)
- Business tier is highlighted with "Recommended" badge
- Pages are responsive across all breakpoints
- Semantic HTML structure throughout
</success_criteria>

<output>
After completion, create `.planning/phases/04-individual-product-pages/04-02-SUMMARY.md`
</output>
