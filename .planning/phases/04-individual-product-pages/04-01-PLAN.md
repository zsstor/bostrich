---
phase: 04-individual-product-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/pages/products.astro]
autonomous: true
requirements: [PROD-01, PROD-02]
user_setup: []

must_haves:
  truths:
    - "Products catalog page displays all 7 products in grid layout"
    - "Each product card shows name, tagline, and description"
    - "Product cards link to individual product pages"
    - "Grid layout is responsive (1 column mobile, 3 columns desktop)"
  artifacts:
    - path: "src/pages/products.astro"
      provides: "Products catalog page with grid layout"
      min_lines: 60
  key_links:
    - from: "src/pages/products.astro"
      to: "src/lib/data.ts"
      via: "getAllProducts() import"
      pattern: "getAllProducts\\(\\)"
    - from: "Product cards"
      to: "Individual product pages"
      via: "slug-based links"
      pattern: "/products/\\{slug\\}"
---

<objective>
Create products catalog page at `/products` that displays all 7 products in a responsive grid layout with product cards linking to individual product pages.

Purpose: Provide users with a browsable catalog of all Bostrich products, making it easy to discover and navigate to detailed product information.

Output: Products catalog page (src/pages/products.astro) with 7 product cards in 3-column grid layout.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-individual-product-pages/04-UI-SPEC.md

# Product data structure from src/lib/data.ts
```typescript
export interface Product {
  id: string;
  name: string;
  slug: string;
  tagline: string;
  description: string;
  features: string[];
  pricing?: {
    free?: string;
    basic?: string;
    business?: string;
    enterprise?: string;
  };
}

export function getAllProducts(): Product[]
```

# Pattern from src/pages/pricing.astro (Phase 3, Plan 03-03)
- Imports getAllProducts() from src/lib/data.ts
- Uses products.map() to iterate over products
- Grid layout: grid-cols-1 md:grid-cols-4 gap-6
- Product sections with name and tagline

# Pattern from src/pages/index.astro (Phase 3, Plan 03-01)
- Featured products grid with 4 products
- Grid layout: grid-cols-1 md:grid-cols-3 gap-8
- Product cards with border, hover shadow, padding
- Card content: product name, tagline, description

# Layout.astro from src/layouts/Layout.astro (Phase 1, Plan 01-03)
- Accepts title and description props
- Provides Header and Footer components
- Container pattern: max-w-7xl mx-auto px-4 sm:px-6 lg:px-8
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create products catalog page with grid layout</name>
  <files>src/pages/products.astro</files>
  <action>Create src/pages/products.astro with the following structure:

1. Import Layout from '../layouts/Layout.astro' and getAllProducts from '../lib/data.ts'
2. Call getAllProducts() to get product array
3. Render Layout component with title="Products — Bostrich Enterprise Solutions" and description="Browse our catalog of vertical mobility solutions, offboarding accelerators, and growth infrastructure for modern enterprises."
4. Inside main section (max-w-7xl container, py-16 md:py-24):
   - h1 heading: "Products" (text-4xl md:text-6xl font-bold, centered, mb-12)
   - Conditional rendering for empty state (if products.length === 0): show error message
   - Product grid (grid grid-cols-1 md:grid-cols-3 gap-8):
     - Map over products array using products.map((product) => ())
     - Each product card:
       - Card container: bg-white border border-gray-200 rounded-lg p-6 hover:shadow-md transition
       - Product name: text-2xl font-bold text-gray-900, mb-2
       - Product tagline: text-lg text-indigo-600 font-medium, mb-4
       - Product description: text-gray-600, mb-4
       - "Learn More →" link: text-indigo-600 font-medium hover:text-indigo-700, href={`/products/${product.slug}`}

5. Ensure semantic HTML: main, h1, section, a elements
6. Use design tokens for colors and spacing
7. Verify responsive layout (1 column mobile, 3 columns desktop)</action>
  <verify>npm run build succeeds and src/pages/products.astro exists with 7 product cards in grid layout</verify>
  <done>Products catalog page displays all 7 products in responsive grid layout with "Learn More" links to individual pages</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → Product slug | User-controlled slug in URL path used to query product data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | Product slug parameter | accept | No authentication/authorization on catalog page - public read access only |
| T-04-02 | Tampering | Product data array | accept | Static data at build time, no user modification possible |
| T-04-03 | Information disclosure | Product details | accept | All product data is public - no sensitive information exposed |
| T-04-04 | Denial of service | Page load performance | mitigate | Static site generation, cached CDN delivery, no database queries |
| T-04-05 | Elevation of privilege | Product slug traversal | accept | No privilege elevation possible - read-only catalog page |
</threat_model>

<verification>
- Products catalog page builds without errors
- All 7 products display in grid layout
- Product cards show name, tagline, and description
- "Learn More" links point to correct product slugs (/products/ladder, /products/agile-ladder, etc.)
- Grid layout is responsive (1 column on mobile, 3 columns on desktop)
- Page title and meta description are set correctly
- Empty state handling exists if products array is empty
</verification>

<success_criteria>
- Products catalog page at /products displays all 7 products
- Grid layout is responsive (1 column mobile, 3 columns desktop)
- Each product card shows name, tagline, and description
- "Learn More" links navigate to individual product pages
- Page uses semantic HTML and design tokens
- Build succeeds with static generation
</success_criteria>

<output>
After completion, create `.planning/phases/04-individual-product-pages/04-01-SUMMARY.md`
</output>
