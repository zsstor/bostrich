---
phase: 06-deployment-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - astro.config.mjs
  - package.json
  - package-lock.json
  - public/robots.txt
autonomous: true
requirements:
  - DEPLOY-05
  - DEPLOY-06

must_haves:
  truths:
    - "Sitemap.xml is generated and accessible in the build output"
    - "Robots.txt exists in the build output with correct directives"
    - "Build succeeds with sitemap and robots.txt included"
  artifacts:
    - path: "astro.config.mjs"
      provides: "Sitemap integration via @astrojs/sitemap"
      contains: "sitemap"
    - path: "public/robots.txt"
      provides: "SEO robots directives"
      contains: "Sitemap"
    - path: "package.json"
      provides: "@astrojs/sitemap dependency"
      contains: "@astrojs/sitemap"
  key_links:
    - from: "astro.config.mjs"
      to: "@astrojs/sitemap"
      via: "import and integration"
      pattern: "sitemap\\(\\)"
    - from: "public/robots.txt"
      to: "sitemap-0-index.xml"
      via: "Sitemap directive"
      pattern: "Sitemap:.*sitemap"
---

<objective>
Add sitemap generation and robots.txt to prepare the site for SEO and deployment.

Purpose: Search engines need a sitemap to discover all pages and a robots.txt to understand crawl rules. These are standard SEO requirements that must be in place before launch.

Output: Astro configured with @astrojs/sitemap integration, robots.txt with proper directives, verified build output.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current Astro config -->
From astro.config.mjs:
```javascript
import tailwindcss from '@tailwindcss/vite';
import { defineConfig } from 'astro/config';

export default defineConfig({
  output: 'static',
  vite: {
    plugins: [tailwindcss()],
  },
});
```

<!-- Site URL required for sitemap -->
Site URL: https://bostri.ch

<!-- Build output structure -->
From dist/:
- about/index.html
- contact/index.html
- index.html
- pricing/index.html
- products/index.html
- products/[slug]/index.html (7 product pages)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @astrojs/sitemap integration to Astro</name>
  <files>package.json, package-lock.json, astro.config.mjs</files>
  <read_first>
    - astro.config.mjs (current configuration to add integration)
    - package.json (current dependencies)
  </read_first>
  <action>
1. Install the sitemap integration:
   ```
   npm install @astrojs/sitemap
   ```

2. Update astro.config.mjs to add the sitemap integration with the site URL. The site URL MUST be `https://bostri.ch`:
   ```javascript
   // @ts-check
   import tailwindcss from '@tailwindcss/vite';
   import sitemap from '@astrojs/sitemap';
   import { defineConfig } from 'astro/config';

   // https://astro.build/config
   export default defineConfig({
     site: 'https://bostri.ch',
     output: 'static',
     integrations: [sitemap()],
     vite: {
       plugins: [tailwindcss()],
     },
   });
   ```

   Key changes:
   - Import `sitemap` from `@astrojs/sitemap`
   - Add `site: 'https://bostri.ch'` (REQUIRED for sitemap — provides the base URL)
   - Add `integrations: [sitemap()]` array
   - Keep existing `output: 'static'` and Vite config unchanged

3. Run build and verify sitemap is generated:
   ```
   npm run build
   ```
   Check that `dist/sitemap-0-index.xml` exists and contains URLs for all 12 pages (home, about, pricing, contact, products, and 7 product pages).
  </action>
  <acceptance_criteria>
    - `astro.config.mjs` contains `import sitemap from '@astrojs/sitemap'`
    - `astro.config.mjs` contains `site: 'https://bostri.ch'`
    - `astro.config.mjs` contains `integrations: [sitemap()]`
    - `package.json` contains `"@astrojs/sitemap"` in dependencies
    - `npm run build` exits with code 0
    - `dist/sitemap-0-index.xml` file exists after build
    - `dist/sitemap-0-index.xml` contains `https://bostri.ch` URLs
  </acceptance_criteria>
  <verify>
    <automated>npm run build && test -f dist/sitemap-0-index.xml && grep -q "bostri.ch" dist/sitemap-0-index.xml</automated>
  </verify>
  <done>@astrojs/sitemap installed and configured. Build generates sitemap-0-index.xml with all page URLs.</done>
</task>

<task type="auto">
  <name>Task 2: Create robots.txt and verify complete build</name>
  <files>public/robots.txt</files>
  <read_first>
    - astro.config.mjs (verify site URL is set — needed for sitemap URL in robots.txt)
  </read_first>
  <action>
1. Create `public/robots.txt` with the following content. This file will be copied to dist/ during build:

   ```
   User-agent: *
   Allow: /

   Sitemap: https://bostri.ch/sitemap-0-index.xml
   ```

   This configuration:
   - Allows all crawlers to access all pages
   - Points crawlers to the sitemap generated by @astrojs/sitemap (which outputs as `sitemap-0-index.xml`)

2. Run a complete build and verify the output includes both files:
   ```
   npm run build
   ```

3. Verify the build output contains:
   - `dist/robots.txt` with the content above
   - `dist/sitemap-0-index.xml` with page URLs
   - All 12 HTML pages still present (home, about, pricing, contact, products, 7 product pages)

4. Verify robots.txt content is correct:
   ```
   cat dist/robots.txt
   ```
   Must show the exact content created above.
  </action>
  <acceptance_criteria>
    - `public/robots.txt` exists with content `User-agent: *` and `Allow: /`
    - `public/robots.txt` contains `Sitemap: https://bostri.ch/sitemap-0-index.xml`
    - `npm run build` exits with code 0
    - `dist/robots.txt` exists after build
    - `dist/sitemap-0-index.xml` exists after build
    - Build generates all pages without errors
  </acceptance_criteria>
  <verify>
    <automated>npm run build && test -f dist/robots.txt && test -f dist/sitemap-0-index.xml && grep -q "Sitemap:" dist/robots.txt && grep -q "bostri.ch" dist/sitemap-0-index.xml</automated>
  </verify>
  <done>robots.txt created with proper directives. Build output includes robots.txt, sitemap-0-index.xml, and all HTML pages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time only | No runtime trust boundaries — purely static file generation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | I | robots.txt | accept | No sensitive paths to protect — public static site with no admin areas |
| T-06-02 | S | sitemap URLs | accept | All URLs are public pages — sitemap merely helps discovery |
</threat_model>

<verification>
1. `npm run build` completes without errors
2. `dist/robots.txt` exists and contains `User-agent: *`, `Allow: /`, and `Sitemap:` directive
3. `dist/sitemap-0-index.xml` exists and contains `https://bostri.ch` URLs
4. All 12 pages built (check `ls -R dist/` for HTML files)
5. No build warnings or errors in console output
</verification>

<success_criteria>
- @astrojs/sitemap installed and configured in astro.config.mjs
- site URL set to `https://bostri.ch` in Astro config
- robots.txt in public/ with correct directives and sitemap reference
- Build succeeds and output includes both sitemap-0-index.xml and robots.txt
- All 12 pages generated without errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-deployment-launch/06-01-SUMMARY.md`
</output>
